name=my project
description=An sbt template for a single-project w/ coverage and docs

organization=com.acme
package=$organization;format="lower,package"$

sbt_version = maven(org.scala-sbt, sbt)
scala_version = maven(org.scala-lang, scala-dist)

user_name = aaronp